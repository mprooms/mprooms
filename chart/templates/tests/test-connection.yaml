apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "mprooms.fullname" . }}-test-connection"
  labels:
{{ include "mprooms.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args:  ['{{ include "mprooms.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
